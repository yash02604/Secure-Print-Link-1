# Suppress unnecessary npm warnings during install
legacy-peer-deps=true
audit=false
fund=false

# Optimize install performance
prefer-offline=false
save-exact=false

# Suppress deprecation warnings from transitive dependencies
# (These are from react-scripts dependencies, not our code)
